# 这里已经没有服务了，文件保留只为记录去向。
#
# 数据库：2026-09-21 从本项目独有的 traveller-db(:5433) 迁到了跨项目共享实例
# shared-postgres(:5432)，见 ~/code/github/moeay1/infra/docker-compose.yml。
# 库名和角色都没变（traveller），只有端口从 5433 改成 5432。
services: {}
