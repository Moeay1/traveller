# 本地 Postgres（docker compose up -d 起的那个，宿主机端口 5433 避开系统已装的 5432）
DATABASE_URL="postgresql://traveller:traveller@localhost:5433/traveller?schema=public"

# 会话 JWT 签名密钥，至少 32 字符。生成：openssl rand -base64 32
AUTH_SECRET="请替换成你自己的随机串至少三十二个字符长度"
