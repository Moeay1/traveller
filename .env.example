# 共享 Postgres（~/code/github/moeay1/infra/docker-compose.yml，容器 shared-postgres:5432）
DATABASE_URL="postgresql://traveller:traveller@localhost:5432/traveller?schema=public"

# 会话 JWT 签名密钥，至少 32 字符。生成：openssl rand -base64 32
AUTH_SECRET="请替换成你自己的随机串至少三十二个字符长度"
